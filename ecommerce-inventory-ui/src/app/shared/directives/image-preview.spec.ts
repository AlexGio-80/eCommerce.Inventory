import { ImagePreview } from './image-preview';

describe('ImagePreview', () => {
  it('should create an instance', () => {
    const directive = new ImagePreview();
    expect(directive).toBeTruthy();
  });
});
